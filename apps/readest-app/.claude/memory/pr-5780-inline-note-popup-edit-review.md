---
name: pr-5780-inline-note-popup-edit-review
description: "PR #5780 (#4668 edit note from bubble popup) MERGED e83fec7f2 after review + Chrome/Xiaomi verify + rebase; CI break = strict ESM vi.mock factory; note bubbles NOT drawn on initial load is PRE-EXISTING on main (no issue filed yet)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 42a7100f-a204-428a-b762-2d2e26ef7782
  modified: 2026-08-20T18:08:52.313Z
---

**MERGED** 2026-08-20T17:44Z as `e83fec7f2` (squash); worktree `readest-pr-5780` and local branches `pr-5780`/`pr-5780-push` removed. Still open: the pre-existing bubble-not-drawn-on-load bug below has NO issue filed yet.

PR #5780 by libbybar (`feature/inline-note-editing`, fork remote `libbybar`, HTTPS) adds Edit/Save/Cancel to the note-bubble popup (`AnnotationNoteItem.tsx`) and extracts `useInlineTextEditor` + `useSaveBooknoteNoteText` + `updateBooknoteNoteText` out of `BooknoteItem`. Reviewed 2026-08-21 in worktree `/Users/chrox/dev/readest-pr-5780` (branch `pr-5780` is REBASED onto main by `worktree:new`; the pushable branch is `pr-5780-push` = real PR head 222e46e03 + cherry-picked fix, see [[worktree-new-rebases-pr-force-push]]).

- CI `test_web_app (1)` failure is REAL and caused by the PR: `annotation-popup-layout.browser.test.tsx` mocks `annotatorUtil` with a factory exporting only `getHighlightColorLabel`; the PR's new chain AnnotationPopup -> AnnotationNotes -> AnnotationNoteItem -> useSaveBooknoteNoteText imports `applyNoteBubbleTransition`/`decideNoteBubbleTransition`, and browser-mode vi.mock is strict ESM, so the whole file fails to import. Fix = add both stubs to the factory (reproduced + verified locally, 3/3 pass).
- CodeRabbit "clearing a note leaves an empty card" is FALSE: `Annotator.tsx` effect (~line 1185) rebuilds `annotationNotes` from `config.booknotes` filtered to non-blank notes, so the cleared note unmounts and the popup falls back to the toolbar. VERIFIED on Xiaomi: clear -> toolbar shown, bubble removed, highlight kept.
- VERIFIED Chrome (web, Alice book in user's library; note restored to its original "Chapter II" afterwards) and Xiaomi (PR APK, Mishima EPUB): bubble tap -> popup -> Edit (real CDP touch) -> textarea autofocus, soft keyboard opens, popup card stays above the keyboard (visualViewport 872 -> 535, card y=189..273) -> typed -> Save -> card + store update, persists across reload. Test highlight deleted on device afterwards.
- PRE-EXISTING (Annotator.tsx untouched by the PR): note bubbles are NOT drawn on initial load / section re-render. `onCreateOverlay` re-adds only `style` annotations; bubbles come only from the `progress` effect (line ~1130), which fires before the overlayer exists. Seen on BOTH Chrome and Xiaomi with an existing note; the bubble appears only after a Notebook/popup save. Worth a separate issue.
- Escape in the popup editor = cancel edit AND dismiss the popup (`Popup` uses `useKeyDownActions` on window); not a regression, left as-is.
- Device APK gotcha: `pnpm dev-android` installed fine but ~5 min later the MAIN repo's older APK (Aug 20 build) was re-installed on the device (md5 mismatch), so the first device pass ran OLD code (no Edit button in the popup DOM). Always md5-compare `pm path` APK vs the worktree APK before trusting a device result.
- Pre-push hook gotcha: `tsgo` failed on stale `apps/readest-app/.next/types/validator.ts` (generated by dev-web on the rebased branch, referencing `src/app/player/page` which the older PR head lacks); `rm -rf .next` before pushing from a worktree whose checkout moved to an older base.

**Why:** Browser-mode `vi.mock` factories are strict ESM; any PR that adds an import along a mocked module's consumer chain breaks unrelated browser tests with "does not provide an export named".

**How to apply:** When a PR fails only `test_web_app (1)` with that message, check the test's `vi.mock` factory for the named export, not the source. Prefer `importOriginal` spreads or add stubs.

**Update 2026-08-20:** #5805 (note popup markdown, [[note-popup-markdown-5785]]) merged first (`841b3639b`) and rewrote the same `AnnotationNotes.tsx` hunk: the card now renders `parseNoteMarkdown(note.note)` (sanitized) via `prose prose-sm max-w-none` + memoized `notesHtml`. PR #5780 will conflict on rebase and must port that render into `AnnotationNoteItem.tsx` instead of `{note.note}`.

**Update 2026-08-21 (rebase done):** rebased `pr-5780-push` onto `841b3639b` (user asked "rebase first", not merge). Conflicts: `BooknoteItem.tsx` import block (keep `removeBookNoteOverlays` + `parseNoteMarkdown` + the two hook imports, drop Marked), `AnnotationNotes.tsx` (take PR side, drop `notesHtml`), and ported the markdown render into `AnnotationNoteItem.tsx` (`noteHtml = useMemo(parseNoteMarkdown)` + `prose prose-sm max-w-none` div) inside the feat commit; main's `AnnotationNotesMarkdown.test.tsx` needed the same extra store/translation mocks as `AnnotationNotesSurface.test.tsx`. Targeted tests 193/193, lint green, browser popup test 3/3; force-pushed with lease (old head 1e64c00a8 -> new head 7f29d4f88, PR MERGEABLE/CLEAN). The HTTPS `libbybar` remote REJECTED the rebased push ("OAuth App ... workflow `.github/workflows/android-e2e.yml` without `workflow` scope") because the rebase carries main's workflow commits; pushed via `git@github.com:libbybar/readest.git` instead (see [[push-workflow-file-needs-ssh-not-gh-oauth]]). Any future rebase-push of a fork PR needs the SSH URL.


## Index status as of 2026-08-24 (moved verbatim from MEMORY.md)
- [PR #5780 edit note from bubble popup (#4668)](pr-5780-inline-note-popup-edit-review.md) MERGED `e83fec7f2`; note bubbles NOT drawn on initial load = PRE-EXISTING main bug (NO issue filed)
