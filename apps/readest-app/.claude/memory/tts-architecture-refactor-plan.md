---
name: tts-architecture-refactor-plan
description: "TTS provider/cache refactor SHIPPED as PR#5126: SpeechProvider seam, per-book SQLite cache, section packs, premium-gated offline downloads"
metadata: 
  node_type: memory
  type: project
  originSessionId: 953eb6c3-86a7-43e6-b56e-5b14f2eacb28
---

**SHIPPED 2026-07-15 as PR#5126** (squash-merged, commit c8a3f85a8; the 15-commit dev arc collapsed to one). Delivered: `SpeechProvider` seam (`BufferedTTSClient` owns scheduler/playout/word-tracking/preload; `EdgeSpeechProvider` owns the Edge fetch); per-book SQLite cache at `Cache/tts-cache/<hash>/cache.db` (tauri-plugin-turso native, turso WASM over OPFS on web; content-addressed by provider/lang/voice/pitch/text so rate is excluded and audio replays at any speed; every failure degrades to plain synthesis); section packs (fully-cached sections compact into one range-addressable, still-playable MP3 with a portable sidecar synced via the file-sync provider; identity = md5 of ordered keys); podcast-style per-chapter offline downloads in the player sheet (headless `TTSDownloader` warms sections off the playback path); bounded-skip offline playback (a non-permanent miss skips the sentence so cached neighbours play; stop only after >3 consecutive unreachable, a cached hit resets it); and premium gating (`isTTSCacheAllowed`/`TTS_CACHE_PLANS` in `utils/access.ts`, mirroring cloud sync -- free/signed-out see a Premium badge + upgrade route, automatic playback cache ungated). Key files: `services/tts/{BufferedTTSClient,EdgeTTSClient,TTSController,TTSDownloader}.ts`, `services/tts/providers/{cache,edge,sqliteCacheStore,bookCacheStore,opfsPackFs,cacheSweep}.ts`, `services/sync/file/ttsPackSync.ts`, `app/reader/components/tts/{TTSPlayerSheet,TTSChaptersView,DownloadBadge}.tsx`, `app/reader/hooks/useTTSDownloads.ts`. OPEN follow-up: confirm a fully-cached chapter opens offline on device/prod (dev can't truly go offline; online HITs proved keys/cache are correct, so the suspect is the fresh per-book store opening under offline). Row label chosen as "Offline Audio".

TTS architecture research (2026-07-13) concluded the <audio> -> WebAudio -> iOS-native playout evolution was right: synthesis and playout are separate planes; playout is a per-platform driver (WebAudio on Web/Desktop/Android, AVPlayer on iOS for session ownership). Do NOT go native on more platforms.

**Approved follow-up refactor (user: "After PR is merged, we will refactor as described above")**, full doc at `apps/readest-app/.claude/plans/2026-07-13-tts-architecture-research.md` (gitignored, local only):
1. Declare `TTSAudioPlayer` + `TTSCapabilities` interfaces (type-only).
2. Extract `SpeechProvider` contract from EdgeSpeechTTS; EdgeTTSClient becomes `BufferedTTSClient(provider)`. Invariants: rate never sent to provider (playout applies it; keeps cache rate-independent); Edge boundary wire shape.
3. `CachingProvider` + `CacheStore` (Tauri fs first, web OPFS later); key hash(providerId, voice, pitch, text); per-provider `cacheable` flag (licensing).
4. Readest Voice provider ([[selfhosted-premium-tts-plans]]) validates the seam.
5. iOS playout_enqueue by cache-file path (drop base64).
6. Local-model provider (desktop first), then maybe synth-to-buffer for system voices (iOS boundaries reconstructable during AVSpeechSynthesizer.write; Android onRangeStart lacks timestamps in file mode).

**Why:** all planned engines differ only in fetch + boundary format; scheduler/WSOLA/word-tracking should be written once. See [[ios-tts-media-session-native]] for the playout saga and session invariants.

**PROGRESS (2026-07-13, on dev, LOCAL commits):** phase 1 `5645a106f` (SpeechProvider seam: providers/{types,edge}.ts, BufferedTTSClient, EdgeTTSClient thin subclass keeping 'edge-tts' name + wss->https fallback, TTSAudioPlayer + TTSCapabilities replacing supportsWordBoundaries); phase 2 `b9f4e8321` (CachingProvider + PER-BOOK SQLite store, user decisions: sqlite not files, one db per book at Cache/tts-cache/<hash>/cache.db, section-pack merge designed in plans/2026-07-13-tts-cache-sqlite-packs.md — concatenated sentence MP3s are range-addressable AND playable). Wired Tauri-only in EdgeTTSClient; controller now retains bookKey from attachView. localStorage 'readest-tts-cache' {enabled,budgetMB=200}. GOTCHAS: node Buffer fails cross-realm instanceof Uint8Array (use ArrayBuffer.isView); tauri IPC blob round-trip UNVERIFIED on device; NodeDatabaseService(':memory:') works in vitest. Follow-up `de0b03817`: cache enabled on WEB too (appService.openDatabase = turso WASM/OPFS, path flattened to one handle name; user caught my wrong Tauri-only gate); purge delete removes Cache/tts-cache/<hash>. PLACEMENT DECISION: cache stays in Cache/ NOT Books/<hash>/ — backupService WALKS Books/ into backup zips + Books is OS-backed-up; Caches excluded + purgeable; non-purge deletes keep cache (warm re-download, mirrors config.json). Phase 3 `6addf390f`: PackCompactor + manifests SHIPPED. Manifest identity = timeline ORDINAL (mark names restart per foliate block; text/lang re-derivation would drift vs parseSSMLMarks cleaning + script inference) — dispatchSpeakMark now RETURNS {sectionIndex, sentenceIndex} located via indexOfRange; client records key at chunk-start (= fully LISTENED sections pack). ensureTimeline registers manifests. Pack tx flips entries blob->(pack,offset,length); self-heal on lost file; two-tier LRU eviction (oldest of loose row vs whole pack); gc sweep of tmp/stray pack files on open; compact debounced 30s + at close. PackFs injected: plugin-fs/AppCache native ($APPCACHE/**/* scope exists), in-memory in tests, absent on web (packed rows = miss). Sweep `b4084b4db`: cross-book budget at book granularity (meta.json last-use stamps on open/close; oldest-first whole-dir deletes; active book exempt; 10-min grace for other windows' live sessions). Settings UI `a9f5a65b3` (TTSPanel Audio Cache section: switch + budget select 50MB-1GB; Manage Cache flow already clears Cache base). Web OPFS packs `1b4879f1c` (TTSPackFs over OPFS; rename=copy+delete since OPFS lacks rename; real-OPFS browser tests). Pack sync phase A `13267c4b0`: pack identity now md5(ordered KEYS) not mark names (voice-collision fix, prereq for sync: same name = same bytes); <packname>.json sidecars (version/section/keysFingerprint/totalSize/entries with ranges+boundaries+durations, written AFTER mp3); importPack(bytes, sidecar) validates + adopts via the shared transactional path; packs.keys_fingerprint column with additive ALTER migration; GC + eviction sidecar-aware. Full sync design in plans doc SECTION 9 (remote layout Readest/books/<hash>/tts/, push-after-compact, pull-on-tts-start, deletions never propagate, default OFF, third-party providers only). Pack sync phase B `ea51ef000`: services/sync/file/ttsPackSync.ts (pushTTSPacks/pullTTSPacks/getActiveTTSPackSyncProvider); remote Readest/books/<hash>/tts/ (additive to frozen layout via SYNC_BOOK_TTS_DIR); existence-check protocol, mp3-before-sidecar upload, sidecar-heal, pull skips json-without-mp3; sidecar REBUILT from db rows on push (stale files can't poison remote); store gained listPacks/hasPack/readPackBytes/buildPackSidecar; triggers in BookTTSCacheStore (pull on open, push after compact>0 + at close) behind syncEnabled (default OFF, 'Sync Audio Cache' switch in TTSPanel) via DYNAMIC import (static would cycle: settingsStore->constants->EdgeTTSClient->bookCacheStore); rides resolveCloudSyncGate + createFileSyncProvider (third-party only, Readest Cloud excluded). PRE-SYNTHESIS / OFFLINE (design section 10 in plans doc): C1 `b8060d3f9` (offline cache-only init — EdgeTTSClient.init falls to cache-only when probes fail + cache present; EdgeSpeechProvider #available flag → permanent-skip on miss; airplane-critical); C2 `da821bd34` (TTSDownloader headless synth behind SectionEnumerator[controller.getTTSDownloader, fresh doc+TTS per section, labels match ensureTimeline]/CacheWarmer[edge client warmSentence+compactCache]; store getSectionStatuses/totalCacheBytes); C3 `92f1b6b99` (podcast player sheet: services/tts/downloadChapters.ts TOC→section-span mapping via view.resolveNavigation; useTTSDownloads hook; DownloadBadge[4 states, e-ink shape-not-color]; TTSChaptersView; 'chapters' SheetView + entry row). BROWSER-VERIFIED FIXES `5acab110a` (live Chrome debugging — 4 defects left the cache dead): (1) bookKey NEVER SET — playback TTSController is constructed with the view directly; attachView (which binds bookKey) only runs on the background-session REATTACH path, so fresh sessions had bookKey=undefined → getBookHash null → store silently no-ops every read/write (symptom: statuses 0 bytes 0, OPFS has Readest_*.db but no Cache_tts-cache_*). Fix: set ttsController.bookKey=bookKey at creation in useTTSControl (~line 761); warn on no-hash open. (2) SHEET-OPEN FREEZE — getController returned as inline arrow (new identity/render) → useTTSDownloads refresh useCallback churns → effect setState loop on sheet open (isOpen true). Fix: useCallback(() => ttsControllerRef.current, []). (3) `***` SEPARATOR SECTIONS never complete — manifest built from ALL getSentences segments but parseSSMLMarks DROPS punctuation/symbol-only marks (ssml.ts filter) → those ordinals never keyed. Fix: enumerator filters getSentences to SPEAKABLE (same rule) so speakableSegs===marks, contiguous ordinals. Verify log was `speakableSegs N marks N`. (4) CONCURRENT registerSectionMarks → UNIQUE constraint manifest_marks(section,ordinal) — two callers (live ensureTimeline + download) race DELETE-then-INSERT. User rejected app-serialization + noted WAL (already on via turso, doesn't help — single-writer + JS-await interleave). Fix: idempotent per-row UPSERT `ON CONFLICT(section,ordinal) DO UPDATE SET mark=excluded.mark` + trailing `DELETE WHERE ordinal>=marks.length` (preserves recorded key). Also: highlighter guards null visibleRange from stale resolveCFI.anchor; download TTS gets no-op highlighter; ttsPackSync uses gate.backends[0] not removed gate.provider. LESSON: web OPFS turso works but multi-DB + StrictMode need care; console capture flaky under Turbopack — use window globals for browser diagnostics. VERIFIED in Chrome: '1 of 14 offline · 1.2 MB', sections packed:true, no errors.

ENTIRE TTS CACHE/SYNC/OFFLINE FEATURE COMPLETE — 12 commits on dev (5645a106f..92f1b6b99), all LOCAL/unpushed. REMAINING: i18n:extract (deferred — user has in-flight locale edits, key-as-content renders EN meanwhile), iOS pack-path playout, export-section-audio, DEVICE VERIFY (blob IPC + full synth→cache→pack→replay→sync→offline cycle). Not yet a PR. GOTCHA (2026-07-15): `cmd | tail -1 && next` masks exit codes — a tsgo error got committed and needed amending; check gates by exit status, not piped output. REMAINING: iOS pack-path playout (playout_enqueue by pack path + byte range — DO AFTER device verify), export-section-audio from packs, web cleanup gap (flattened OPFS cache.db files not covered by purge/sweep), DEVICE VERIFY (tauri IPC blob round-trip via plugin-turso + live synthesize->cache->compact->replay cycle + pack range reads on iOS).

**PR #5085** (feat/native-tts-playout, 3 squashed commits: CarPlay+tao submodule, Android focus, iOS native playout+volume-key) carries all of it; submodules need fork branches `readest/tauri@readest-2.11` and `readest/tao@readest-0.35` (both pushed 2026-07-13; tao = tag tao-v0.35.3 + UAF fix da30a3b9 ONLY (lint allow reverted; CI fixed via rustflags:'' in pull-request.yml)). Local `dev` still has the unsquashed 9-commit history (tao vendored there, submodule only on the PR branch); after merge, reset dev onto origin/main.
